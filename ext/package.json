{
  "name": "kinopoisk-tmdb-extension",
  "version": "3.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "test": "node --test src/services/__tests__/adapters.test.ts",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.250",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}